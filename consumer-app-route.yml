---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: iot-demo
    name: consumer-app
  name: consumer-app
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: consumer-app
